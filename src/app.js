const a = 1;

const foo = (name) => {
  const lastName = name;
  return lastName;
};

foo('Denis');

const x = 200;

console.log(window);
console.log(x, a);

const greet = (name) => `Hello ${name}`;
greet('Anna');

alert('Hello world');

const obj = {
  name: 'Karol',
};

const obj1 = obj.name;
console.log(obj1);
